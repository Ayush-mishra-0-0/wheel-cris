"""Layer 5 dashboard product backend (FastAPI app + validation engine).

Imports the ML feature extractor / serving models from the ml/ tree via the
`_paths` bootstrap, while keeping this package home in development/dashboard.
"""
